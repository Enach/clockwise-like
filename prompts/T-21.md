# T-21 — Rename app from "Clockwise-like" to "Paceday"

## Goal
Replace every occurrence of "Clockwise-like" (and variants) with **Paceday** / `paceday` across all layers of the project.

## Changes made

### Go modules
- `backend/go.mod`: `module github.com/Enach/clockwise-like/backend` → `module github.com/Enach/paceday/backend`
- `mcp/go.mod`: `module github.com/Enach/clockwise-like/mcp` → `module github.com/Enach/paceday/mcp`
- All 38 Go source files: import paths updated via sed

### Docker / CI
- `docker-compose.prod.yml`: `clockwise-backend` / `clockwise-frontend` → `paceday-backend` / `paceday-frontend`
- `.github/workflows/sync-and-publish.yml`: GHCR image tags updated to `paceday-backend` / `paceday-frontend`

### MCP server
- `mcp/tools.go`: server name `"Clockwise-like"` → `"Paceday"`

### Frontend
- `frontend/index.html`: `<title>Clockwise-like</title>` → `<title>Paceday</title>`
- `frontend/package.json`: `name` → `"paceday"`

### Documentation
- `README.md`: title, description, and MCP section updated

### Anytype
- Space renamed from "Clockwise-like" to "Paceday"
- Space description updated

## Not renamed
- PostgreSQL user/db (`clockwise`) — credential, not app branding; renaming would break existing volumes
- `smart-calendar-flow` GitHub repo — external repo, rename via GitHub UI
- Prompt history files — kept as-is per "keep git history intact" guidance
