You are an expert Go and React developer. Initialize the clockwise-like project from scratch.

Repository: git@github.com:Enach/clockwise-like.git
The repo already exists but is empty. Clone it and set up the following structure.

## Task
Bootstrap the full monorepo structure for a Clockwise-like calendar management app.

IMPORTANT: The frontend React source code (src/) will be generated separately by Lovable.
Create only the shell structure needed for Docker to build — use placeholder pages for now.
The actual frontend UI will replace src/ later.

### Tech stack
- Backend: Go 1.22, chi router, SQLite (modernc.org/sqlite)
- Frontend shell: React 18, TypeScript, Vite 5, Tailwind CSS (placeholder pages only)
- Infrastructure: Docker Compose, Nginx reverse proxy

### What to create

1. **Root files**
   - `docker-compose.yml` — backend on :8080, nginx on :80, SQLite data volume
   - `.env.example` — all env vars (see spec)
   - `.gitignore` — Go, Node, .env, data/, dist/
   - `README.md` — setup instructions (docker compose up --build, set .env, visit localhost)
   - `PROMPT.md` — this exact prompt text

2. **backend/**
   - `Dockerfile` — multi-stage: golang:1.22-alpine builder → alpine:3.19 runtime
   - `go.mod` — module: github.com/Enach/clockwise-like/backend
   - `main.go` — starts HTTP server on $PORT (default 8080), sets up chi router, mounts /api routes
   - `api/routes.go` — register all route groups: auth, settings, calendar, focus, schedule, nlp
   - `api/middleware.go` — CORS (allow all origins), request logging with method + path + duration
   - `api/handlers_health.go` — GET /api/health → { "status": "ok", "version": "0.1.0" }
   - `storage/db.go` — open SQLite at $DB_PATH, run migrations on startup
   - `storage/models.go` — Go structs: Settings, OAuthToken, FocusBlock, AuditLog
   - `storage/migrations.go` — CREATE TABLE IF NOT EXISTS for all 4 tables:
     * settings (id, work_start, work_end, timezone, focus_min_block_minutes,
       focus_max_block_minutes, focus_daily_target_minutes, focus_label, focus_color,
       lunch_start, lunch_end, protect_lunch, buffer_before_minutes, buffer_after_minutes,
       compression_enabled, auto_schedule_enabled, auto_schedule_cron,
       llm_provider, llm_model, llm_api_key, llm_base_url, updated_at)
     * oauth_tokens (id, access_token, refresh_token, expiry, calendar_id, updated_at)
     * focus_blocks (id, google_event_id UNIQUE, start_time, end_time, date, created_at)
     * audit_log (id, action, details, created_at)

3. **frontend/** — placeholder shell only (Lovable will replace src/)
   - `Dockerfile` — node:20-alpine build stage → copy dist/ into nginx:alpine
   - `package.json` — all required deps:
     react, react-dom, react-router-dom v6, @tanstack/react-query,
     tailwindcss, postcss, autoprefixer, lucide-react, axios,
     @fullcalendar/react, @fullcalendar/timegrid, @fullcalendar/interaction, @fullcalendar/daygrid
   - `vite.config.ts` — proxy /api → http://backend:8080 (for local dev), build outDir: dist
   - `tsconfig.json` — standard React + TypeScript config
   - `index.html` — standard Vite entry
   - `tailwind.config.js` + `postcss.config.js`
   - `src/main.tsx` — minimal React render
   - `src/App.tsx` — placeholder: shows "Clockwise-like — Loading..." (will be replaced by Lovable)

4. **nginx/**
   - `nginx.conf` — serve frontend dist/ on :80, proxy /api/ to backend:8080

### Commit instructions
- Commit 1: "T-01: Backend scaffold — Go, SQLite migrations, health check"
- Commit 2: "T-01: Frontend shell — Vite/React placeholder, Dockerfile"
- Commit 3: "T-01: Docker Compose + nginx config"
- Include prompts/T-01.md with this prompt text
- Push to origin

### Verification
`docker compose up --build` should:
- Serve the placeholder React app at http://localhost
- Return {"status":"ok","version":"0.1.0"} at http://localhost/api/health
