You are an expert Go engineer and prompt engineer. Replace the basic NLP system prompt with a rich, timezone-aware scheduling prompt that accounts for all participants' working hours and timezones.

## Context

The current system prompt only knows the user's own timezone and working hours. This task replaces the prompt template with a richer version that uses participant data, and updates the NLP flow to a two-pass approach.

## New files

### nlp/prompts.go

Defines:
- `SchedulingPromptData` — runtime data injected into the template
- `ParticipantSummary` — per-participant view (timezone, working hours, local time)
- `BuildSchedulingPromptData(settings, participants, now)` — builds the template data
- `SchedulingSystemPromptTmpl` — rich system prompt with participant timezone rules
- `renderSchedulingPrompt(data)` — template execution helper

### calendar/participants.go

Defines:
- `ParticipantInfo` — resolved timezone and working-hours info for a participant
- `ResolveParticipants(emails)` — returns basic info for each email (timezone resolution to be enhanced)

### nlp/prompts_test.go

Tests: SingleTimezone, MultipleTimezones, UnknownTimezone, FarTimezone, TemplateRenders

## Updated files

### nlp/parser.go

- Two-pass NLP: Pass 1 extracts attendees (lightweight prompt), Pass 2 uses full participant-aware prompt
- ParseResult extended with: PreferredTimes, AvoidTimes, TimezoneNotes, ParticipantInfos
- ScheduleRequest extended with: PreferredTimes, AvoidTimes (passed to smart scheduler)

### engine/smart_schedule.go

- ScheduleRequest: added PreferredTimes and AvoidTimes fields
- adjustScoreForPreferences: preferred times +25 pts, avoid times -40 pts
- timeInHHMMRange + parseHHMMParts helpers

## Commit

`T-12: Timezone-aware scheduling prompt — participant working hours + timezone context`
